spring:
  datasource:
    password: ${PGPASSWORD}
    url: ${DATABASE_URL}
    username: ${PGUSER}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true